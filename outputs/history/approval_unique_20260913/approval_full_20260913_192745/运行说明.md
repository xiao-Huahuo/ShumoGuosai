# 支撑材料运行说明

本目录保留项目相对结构。阅读结果可先打开 `outputs/final/` 中的五份Excel，或使用各问原始CSV、HTML报告；这一步无需安装求解器。第三问和第四问存在不同源码版本，重跑前应选择与所要核验结果对应的版本。所有正式文件均为已保存结果的副本，重新计算请使用新的输出目录，避免把收集材料与新结果混在一起。

| 材料 | 源程序与环境 | 已保存结果及使用说明 |
| --- | --- | --- |
| 第一问 | `src/q1/`，依赖 `src/q1/requirements.txt` | `outputs/.q1-runs/run-ie6fkatf/`；现有程序入口 `python src/q1/run.py --no-plots` 会创建新的运行代。历史精确版本的缺口见源码版本核对表。 |
| 第二问定稿 | `src/q2/`，依赖 `src/q2/requirements.txt` | `outputs/q2/dispatch_runs/20260912_tail_reserve_final/`；冻结预测在其 `raw/shadows/`，逐日计划与审计在 `processed/main/`。入口及参数见 `docs/2/tail_reserve_work/run_guide.md`，旧来源记录与当前源码差异见核对表。 |
| 第三问正式结果 | `outputs/q3/raw/full_priority_rescue_7h_20260912/runtime/src/q3/` 及同级 `requirements.txt` | 从冻结runtime目录运行，以环境变量 `Q3_WORKSPACE_ROOT` 指向本材料根目录。该运行继承 `outputs/q2/dispatch_runs/20260911_224501/` 的预测和选择记录；相关真实输入已收齐。原 `resume.sh` 仅保留当时命令，不宜原样执行其中的本机绝对路径。 |
| 第四问4-2 | `docs/4/rescue_lp/frozen_source_v2/` 中的源码与依赖清单 | 正式结果为 `outputs/q4/raw/rescue_lp_q42_20260913_v2/`。冻结版本通过 `Q4_WORKSPACE_ROOT` 指定材料根目录；工作目录应选对应冻结源码根，避免误加载当前 `src/q4/`。 |
| 第四问4-3 | `docs/4/rescue_lp/frozen_source_v3/` 中的源码与依赖清单 | 正式结果为 `outputs/q4/raw/rescue_lp_q43_20260913_v3/`，同样使用 `Q4_WORKSPACE_ROOT`。模型和数值审计修订依据保留在 `docs/4/rescue_lp/`。 |
| 局部敏感性 | `src/sensitivity/`，以及实验目录保存的runtime/frozen快照 | 第二问与第三问在 `outputs/sensitivity_q2_q3/raw/local_20260913/`；第四问在 `outputs/q4/raw/sensitivity_20260913/`。各自实验报告和运行说明保留在 `docs/sensitivity_*`。 |
| 科研图 | `src/plots/`；部分图目录另有重绘代码与依赖 | `outputs/processed/figures/` 保存各问图件和数据。保留PNG/SVG可直接使用；重绘所需的中文和TeX字体按对应图说明配置，字体未作为项目附件复制。 |
| 第一问Excel导出 | `src/q1/export_submission.mjs` | 从材料根目录运行，需安装原程序所用 `@oai/artifact-tool`；项目未包含该依赖的独立安装包或锁文件。已导出的 `outputs/final/result1.xlsx` 可直接打开。 |

Python依赖请按所选任务创建独立环境。第一问、第二问和第四问冻结环境的NumPy、SciPy等版本不同，应保留各自已有清单。复制材料不会自动处理旧JSON中的绝对路径或恢复签名；已有验收记录说明的是原运行情况，本次文件验收记录另见 `校验结果.json`。

部分旧绘图入口读取 `outputs/q1_current` 或 `outputs/q2_current`。原项目的这两个位置是文本指针，本材料直接保留其对应真实运行目录，没有把文本指针当成数据。需要运行这些入口时，可在材料根目录建立下面两个相对链接；Windows环境应将对应真实运行目录复制到同名位置，或使用系统支持的目录链接。

```sh
ln -s .q1-runs/run-ie6fkatf outputs/q1_current
ln -s .q2-runs/run-ndjkd2zx outputs/q2_current
```
