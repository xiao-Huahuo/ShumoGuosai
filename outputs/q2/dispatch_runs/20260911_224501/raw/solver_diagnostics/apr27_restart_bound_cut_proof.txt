仅用于2025-04-27/K1/S28/SOC1200的额外诊断。重新从封存输入与预测库构造，逐项断言selection、scenarios、日期、初态与已完成的apr27_monotone_charge_continuous_z.json一致；原候选源码SHA也一致。
前一次1800秒自由单段充电等值模型的HiGHS全局下界L=32989.12162548969。因此该模型每个整数可行解目标都>=L。追加一条-objective <= -(L-1e-5)，保留所有整数可行解，以旧下界减去既有数值容差保守处理；并用已由原反馈复核的flow_cuts策略作可行初值。这是复用既有界，不保存/恢复分支树，不把固定购电局部模型的下界当全局下界。
本次另有最多900秒，之前1800秒以及更早失败仍保留。若本次成功，也必须注明依赖既有额外计算；不得冒充从零900秒通过。此实现仍仅为诊断包装，未接入生产或更改冻结队列执行。是否可普遍采用，需要完整生产入口与证书来源验证。
