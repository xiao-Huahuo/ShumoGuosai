# Q1 最终模型实现计划

## 目标与验收标准
严格实现 `Q1最终优化模型-最终版.md` 全部 15 节：144 时段右端点映射、ZOH 单位换算、母线侧效率、六类变量 MILP、九类物理检查、CSV 写入回读（用户明确替换Excel要求）、四种效率情景。用户最终可查看原始/预处理输入、正式 result1.csv、全量变量、表1/表2、图表、可复现记录及逐条验收报告。

## 阶段
- [x] 阅读模型、AGENTS、开发规范，定位 C 题附件和既有审查代码。
- [x] 核对原始附件和模板，保存输入副本与参数来源。
- [x] 实现独立的最终模型程序，不沿用旧稿的二级目标或简化模型。
- [x] 求解四种效率情景，完成物理、最优性、CSV 回读检查及针对性负向测试。
- [x] 逐句审查、挖洞审查、假设审查、图表/CSV报告实际界面冒烟；完成报告及变更历史。

## 已确认的实施边界
- 代码 `src/q1/`，原始输入 `inputs/q1/raw/`，预处理输入 `inputs/q1/processed/`，原始输出 `outputs/q1/raw/`，整理结果 `outputs/processed/q1/`；新文件名用 ASCII。
- 全部文本 UTF-8 无 BOM；原始附件及模型文档保留不改。
- 主目标只有购电费，不添加二级吞吐量目标；同成本解允许不唯一。
- 主模型双单程效率 0.9；敏感性 0.85、0.95，另做 sqrt(0.9) 口径。

## 待决与错误记录
- 用户已明确确认采用5000/6准确公式，833.3333只作显示。
- 用户已明确改为CSV交付，停止WPS操作，移除本次生成的xlsx输出和模板副本。
- 压力测试发现默认整数容差导致0.000269kWh同时充放电；已收紧HiGHS容差，重跑压力与全部真实情景均通过。
- 一次补丁因同一文件删除/添加重复操作被工具拒绝，未产生部分修改；拆分后已成功。
- 仓库没有 .git，git status 不能执行；通过文件清单及针对性校验跟踪本次产物。
- 已独立核实官方模板时间晚移10分钟；用户改CSV后不再沿用模板。

## 当前状态
全部实现、CSV输出、八组测试、逐条审查、实际报告界面冒烟与文档完成。用户原始最终模型未更改。
