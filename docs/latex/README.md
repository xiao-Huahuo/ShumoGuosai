# 论文最终文件

本目录保存最终 `cumcm.pdf`（486页）、对应的 `cumcm.tex`、4张引用图片、字体、模板样式、参考文献和145份源码附录。最终PDF从桌面的已有文件原样复制，TeX正文与附录源码未改。支撑材料压缩包位于上一级的[支撑材料.zip](../支撑材料.zip)，原项目的 `approval` 目录保持不变。

重新编译请在本目录执行 `latexmk -xelatex cumcm.tex`，所需相对路径均已随工程迁移。编译缓存已清理，以下保留模板原有使用说明。

# CUMCM 2026 LaTeX 模板

本仓库面向中国“全国大学生数学建模竞赛（CUMCM）”。模板按 2026 年全国组委会发布的论文格式规范、参赛规则和 AI 工具使用规定设计。

## 编译

```bash
latexmk -xelatex cumcm.tex
```

主文件是 `cumcm.tex`，公共版式集中在 `cumcm2026.sty`。必须使用 XeLaTeX；工程内 `latexmkrc` 已设置默认引擎。VS Code / LaTeX Workshop 默认配方为 `latexmk (XeLaTeX)`，既支持打开整个项目，也支持单独打开本论文目录。Overleaf 中将 Compiler 设置为 XeLaTeX。使用 AI 时，还需填写 `ai-usage-details.tex`，编译后将 PDF 重命名为 `AI工具使用详情.pdf` 并放入支撑材料。

## 交付模式

- 电子版：直接提交 `cumcm.pdf`；第一页必须是摘要页，不得含承诺书或编号专用页。
- 纸质版：使用组委会或赛区当年下发的承诺书作为第一页、编号专用页作为第二页，再装订与电子版内容格式一致的论文。不要自行仿制这两张官方专用页。
- 支撑材料：另行制作 RAR 或 ZIP；论文附录中的文件清单必须与压缩包一致。

## 投稿前检查

1. A4 白纸；四边页边距均不小于 2.5 cm；纸质版左侧装订。
2. 摘要含标题和关键词，原则上不超过一页；摘要页页码为 1，页脚居中。
3. 正文从摘要后的下一页开始；不设置目录；正文不超过 30 页，附录页数不限。
4. 摘要、正文、附录、PDF 元数据、文件名及支撑材料中均不得泄露姓名、学校或赛区。
5. 参考文献在正文引用处标注，并按科技论文规范列出。
6. “AI工具使用声明”位于参考文献之前，且声明与实际使用情况一致。
7. 附录含支撑材料文件列表和全部完整、可运行的源程序；无程序或无支撑材料时使用官方规定的明确表述。
8. 论文电子版为单个 PDF 或 Word 文件，建议 PDF，不压缩且不超过 20 MB；支撑材料压缩包不超过 20 MB。
9. 论文电子版与纸质版的论文内容及格式（含附录）完全一致。
10. 提交前重新编译、运行代码、核对表图，并检查所在赛区是否有额外要求。

## `tabularx` 自定义列

- `Y`：自动换行、水平居中；
- `R`：自动换行、水平右对齐；
- `L`：自动换行、水平左对齐；
- `C{宽度}`：固定宽度、水平居中。

其中 `Y` 和 `R` 均由 `X` 派生，可按剩余宽度自动伸缩。

## 本项目附录源码

`cumcm.tex` 已载入 `code/src/` 中的 145 份源程序，来自项目 `approval/src/`，按问题一至四、敏感性分析、绘图及补充程序分组。该目录同时保留配套依赖与配置，共 152 份文件。`references.bib` 已同步支撑材料的三项书目，正文沿用现有参考文献列表。2026-09-13 按用户要求快速插入，未编译、未运行程序或检查。

## 编译错误修复（2026-09-13）

修复了误用 pdfLaTeX、附录字体命令放在正文区、手写参考文献与作者年份引用模式不匹配的问题。附录使用随工程提供的 DejaVu Sans Mono 字体，保留源码中的希腊字母、上下标和数学符号；中文字体继续使用随工程提供的宋体/黑体。源码文件与论文正文未修改。若旧失败编译留下损坏的辅助文件，可在本目录执行 `latexmk -c cumcm.tex` 后重新编译。

本轮实际编译已通过：486页，约7.4MB，最终日志无编译错误、未解析引用或缺字；已在macOS预览打开并查看附录末页。尚有少量非致命行宽及黑体加粗替代警告。
