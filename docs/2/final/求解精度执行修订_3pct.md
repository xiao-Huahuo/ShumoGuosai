# 求解精度执行修订：后续3%（2026-09-12）

用户明确命令“那就换成3%，继续跑”，覆盖此前保持2%的要求。本修订只更改单次优化的MIP gap门槛；附带的外部ChatGPT建议不自动转成执行指令。原最终建模文档逐字节保留。

假设与范围：不预测全量完成时间，不把单日实测推广为全年速度；保留此前已执行策略的真实连续轨迹，不声称从第一天统一3%重跑。3%约束的是单次场景优化的可行上界相对全局下界的费用差距，不是全年费用或预测误差。

## 执行

2026-09-12 12:22:45（北京时间）新正式worker启动，活动精度记录为`raw/solver_precision_revision.json`，SHA为`afd9bf5f1f8a78c674ab95f87230b68b6f9309f04bdd56246b78c6f3469602ec`。旧2%修订存入`raw/solver_precision_revision_fb857ad26bff.json`，原1%与2%队列执行记录不覆盖。各阶段策略和末SOC沿用，不重算已提交日。

| 任务 | 保留完整日数 | 从哪天续算 |
| --- | ---: | --- |
| main | 109 | 2025-05-21 |
| horizon_1 | 132 | 2025-06-13 |
| horizon_2 | 93 | 2025-05-05 |
| horizon_3 | 91 | 2025-05-03 |
| predictor_week_mean7 | 5 | 2025-02-06 |
| window_56 | 32 | 2025-03-05 |

K1/K2/K3每日比较、场景池与缩减判据、历史信息边界、10分钟分辨率、物理约束、稳定性阈值保持原样。每次120秒、全日候选受限后900秒补算、两个并发worker也保持原样。本次没有采用固定K2、S20、后续日粗粒度或180秒上限。

主回放和当时运行的K3在保存完整前缀后重排；此前固定K1/K2因2%证书超时失败的任务保留旧收据和失败状态历史，以新3%执行版本从原末SOC重新排队。没有把旧失败直接改为已通过。

## 验收与现状

6项精度传递和断点测试通过，新增1%→2%→3%连续迁移测试验证两个旧阶段的策略、审计和末SOC不变。正式监督preflight的79项完整测试通过；6组共462日完整前缀的输入、电价、物理反馈、费用和SOC连续性离线验收通过。IAB28实际页面已显示3%与计算中。新正式提交日独立复制回读通过，见`raw/precision_3pct_official_validation.json`：主110日（旧109日逐值与审计SHA不变），固定K1 139日（旧132日不变）。新主5月21日选K2，证书gap为2.614518%，已核验原物理反馈、费用、电价、连续SOC及当前源代码身份；新固定K1的7日均满足3%。IAB30实际确认报告修订为3%，数值仍是73日历史快照。

5档精度实测沿用已结束的单日记录，不新增或重复测试。3%用时148.064秒、3/3候选达标，是当时并行负载下单日一次观测，不是全年平均或完成时间保证。

P2：此前切换仅完成准备测试，尚未生效；旧2%计算仍推进到109天。此次已实际重载为3%。P1：全年334日、27项必需实验和最终result2尚未完成；历史计算受限标记保留，最终验收仍须如实披露。没有发现本次精度切换引入的物理或存档故障。

精度版本、462日前缀副本及旧队列状态位于`raw/precision_transition_3pct_20260912T042229Z`。后续实时状态查看`processed/progress.html`，`report.html`的数值仍是生成时快照，不把旧报告的73日当成当前进度。
