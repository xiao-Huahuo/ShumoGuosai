# 第三问求解加速工作记录

本文件记录当前源码、困难节点、求解后端、精确等价性和迁移证据。数值只来自实际运行或现存检查点；待验证结论明确标记。

## 现场冻结

- 用户附件已从微信长期消息目录原字节归档到本目录，双方 SHA-256 均为 `10023c55650d3974dc6d6297dd1f907513c6133c589574137bd5e10c17562795`。原 `temp/drag` 路径读取后失效，未据此猜测内容；重新定位到 `msg/file/2026-09` 后完成字节核对。
- 当前生产运行：`outputs/q3/raw/full_priority_parallel_20260912`；配置仍为 S=20、tail=2、节点 0/6/12/18、120 秒切片、3%、4 个 solver threads。
- 现场检查时已提交 22 个预热日，活动节点 `2025-01-23 06:00`，原 replay 目标 `1458.9025985198678`，有效下界 `1402.7907439937876`，gap `3.846168660129095%`，未达标。检查点封装摘要与内部摘要一致，policy 三个向量均为 144 长。
- worker PID 85000 存活，观测 CPU 约 323%，RSS 约 1.32 GiB。影子测试期间不停止生产。

以上为现场冻结时的历史快照。用户随后在 2026-09-12 22:01 明确要求停止主计算：PID 84991/84996/85000 均已退出；最终保留25个完整预热日，活动节点Jan26/06、8个切片、最后gap约7.9523%。右侧API已核实返回 `status=stopped`、`worker_alive=false`。没有继续运行剩余影子 benchmark。

## 必须保持的验收口径

- 后端比较必须复用同一个 `build_matrix()` 结果并写 matrix digest。
- 报告 objective 必须由原 `physics.replay()` 重算；有效下界与原目标使用现有分母 `abs(objective)` 计算 3% 证书。
- 需要 Jan16、Jan22、当前困难节点三个 06:00 真节点；至少 2/3 显著改善且无灾难性回退才允许迁移。
- Gurobi 无许可证或受尺寸限制时明确 No-Go，继续生产 HiGHS；不能因附件建议而假定许可存在。

## Gurobi 实际检查

- 宿主与 Q3 固定环境原先均未安装 `gurobipy`。使用临时 `uv --with gurobipy` 从公共包仓库加载 13.0.3，未改变正式运行依赖。
- 极小二元模型求解成功；许可证显示 restricted non-production，截止 2027-11-29。
- 将 Jan22 的原 `build_matrix()` 完整矩阵原样映射后，24,732 个变量、39,409 行、6,048 个二元变量；矩阵摘要 `2ff1bab6a35dbd09bdddb46d39de81953a23ef51aeca9238077c9d85f8d70db4`。
- `optimize()` 明确返回 `Model too large for size-limited license`。因此 Gurobi 对当前正式矩阵不可用，按用户指令判定 Gurobi 路线 No-Go，不加入生产依赖、不停止主计算。
- 同矩阵后端和小规模 gap=0 测试仍保留在 `gurobi_backend.py` / `test_gurobi_backend.py`，便于以后许可证变化时复核；本轮生产继续 HiGHS + exact reformulation v2。

## 已拒绝的 min 候选次序 cuts

在 v2 影子分支临时加入每场景槽 6 条“被选候选不大于另外两项”的有效式。Jan16/Jan22/Jan23 连续松弛下界与 legacy 分别逐值相同，但行数净增 5ST，三个 LP 构建求解时间均上升。它没有增强根下界，已从生产 builder 移除；结构测试恢复为 v2 净减 ST 行。失败现象及修正均保留在计划记录中，不作为加速成果。

## 已拒绝的 2-bit selector

完成四模式 2-bit 精确编码并以 36 组 gap=0 小实例验证目标一致；它在 Jan16/Jan22/Jan23 分别把二元变量从 3456/6048/6480 降至 2304/4032/4320。根下界在 Jan22、Jan23 数值相同，但 Jan16 从 3004.701283 降至 3004.419031。附件明确要求二元减少但下界变差时立即放弃，因此未做更多调参，也未把 `v2_2bit` 暴露给 Config 或正式 CLI；builder 仅保留为不可生产的研究证据。
