# 第三问 v4 完整实现与小样本计时

## 目标与约束
按 `Q3_第三问_最终建模_严格修订终稿_v4.md` 逐句落实代码；遵守根目录开发规范；不启动全量。只读复用第二问预测流程，不修改第二问正在运行的代码和产物。UTF-8 保存。

## 阶段与验收
- [x] 阅读开发规范、完整模型、原始模板和第二问接口（持续核对细节）。
- [x] 数据与因果预测、联合场景、精确反馈 MILP、真实滚动执行。
- [x] 有限差分终端价值、FIV/OUV、M0/M2、所有敏感性、模板与诊断。
- [x] 数学性质/泄漏/费用/模板测试；真实小样本限时计时；实际报告界面冒烟。
- [x] 逐句验收、挖洞和假设审查；README 目录、CHANGE_HISTORY 和最终报告。

## 决策
- 正式主模型固定 S=20、尾部2、28/56/84/全部窗口；任何计时不能反选参数。
- 计时保留求解器状态、上下界、gap；限时未证明最优不得宣称最优。
- 本工作区无 Git 仓库，按新增 q3 文件隔离改动。
- 用户2026-09-12明确确认：历史完整24h确定性辅助问题、初态6000/6100、最近28个可用日中位数标定。
- M2每子节点至少2条，父节点至少4条才尝试二分；用于避免单历史轨迹被完整揭示。
- 正式求解gap默认0；15秒是小样本诊断预算，没有继承Q2的3%授权。
- 模板整体10分钟错位修正并保存原表头；调整量写最终生效绝对量，原模板结构扩展到所有正式日期。

## 错误记录
- `git status --short`：当前不是 Git 仓库；不影响文件实现。
- 首次计时因单个历史FD值为零被过严检查拒绝；已允许合法零样本、仍要求最终中位数>0。三个季节四节点中位数均正。
- 真实M0样本零紧急事件导致Excel回读空列被推断为object；已显式按float回读空事件，新增零事件回归。

## 当前状态
代码/23项测试/2118非空源行映射/真实界面核验已完成；12个15秒节点、2个60秒难例及真实M0/M2/FIV组件测试已结束。没有全量运行。严格最优计算可完成性未取证，详情见implementation_report.md。
