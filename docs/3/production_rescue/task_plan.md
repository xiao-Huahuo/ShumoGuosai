# Q3 七小时生产救援计划

## 目标

从已停止的 `full_priority_parallel_20260912` 继承25个完整预热日和Jan26状态，在数学模型、S=20、tail=2、24h、144槽、四节点、HiGHS 1.15.1、4线程和现有fast legacy formulation不变的前提下，以06:00总求解35秒、其余节点总求解8秒的硬时限先生成完整 `result3.xlsx`。

## 执行阶段

- [x] 用户明确授权约7小时直接运行；冻结旧运行并确认无活动worker。
- [x] 实现节点总hard wall与“3%达标/超时可行”双状态，保持原验证门禁。
- [x] 停止独立检验等待进程，确保主轨迹only。
- [x] 运行有界测试：提前达标、超时接受、无可行解拒绝、恢复与导出。
- [x] 新建冻结rescue运行，迁移25日与Jan26政策，旧目录只读保留。
- [x] 启动全年主链，核验PID、硬时限、首个提交日、检查点和右侧看板。
- [x] 同步报告、README、CHANGE_HISTORY及逐条验收。

## 接受规则

- 先尝试原3%证书；达到即标记 `certified_3pct`。
- 到硬时限仍未达3%，只有当前best incumbent通过原 `physics.replay()`、能量平衡、SOC、功率、策略域、矩阵可行性和整数性后，才标记 `hard_limit_feasible` 并继续。
- 无合法可行解则停止，不能编造或接受非法策略。
- 每节点保存真实UB、LB、gap、接受原因和预算；全年报告不得声称所有节点均达到3%。

## 时间预算

剩余340天，每天35+3×8=59秒，纯节点预算上限20,060秒，即5小时34分20秒；另留约1小时25分40秒给场景构造、历史FD、回放、I/O、检查和Excel导出。七小时是执行目标，不是硬件故障情况下的完成保证。

## 当前状态

已完成并通过最终验收。主程序一次启动运行3小时18分39秒，生成365日state和正式334日/48,096行 `result3.xlsx`。日收据SHA、全年物理回放、SOC连续、节点接受门禁与四张工作表逐值回读均通过；真实精度统计见 `acceptance.json`和 `report.md`。本轮未运行任何辅助任务或加速benchmark。

## 错误与修正

- 首次预启动迁移成功后，审查发现兜底时钟容差按2%预算会允许35秒节点提前0.7秒接受。该目录从未启动，保留为`full_priority_rescue_7h_20260912_prelaunch_superseded`；容差改为固定0.05秒并重新测试、重新冻结迁移，避免就地修改runtime。
