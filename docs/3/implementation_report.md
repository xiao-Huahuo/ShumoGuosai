# 第三问 v4 代码实现与计时验收

**后续并行改造已完成：** 默认4进程，4个限时实例33.65秒→9.06秒（3.71倍），FD标定1.88秒→0.55秒；32项合并测试通过，仍未跑全量。[并行报告与命令](parallel/report.md)。下文保留首次单进程建模验收及历史计时，不能直接将全年时长除以该加速比。

代码已按 v4 全部章节实现，23项自动核验通过，完成真实附件组件测试和本地报告界面检查。**没有启动全年运行，没有生成正式 `result3.xlsx`。**

主要时间结论：12个真实24h节点以15秒预算求解，端到端中位数15.315秒；均有通过原反馈物理核验的可行解，但均未证明严格最优。另两个固定6000 kWh初态的实例以60秒预算求解，仍分别有13.53%和4.58%的gap。它们与首轮日内连续状态不同，不能把前后gap直接解释为同一实例的改善幅度。

## 已明确的口径

- 唯一模型依据为本目录 `Q3_第三问_最终建模_严格修订终稿_v4.md`，未修改原文。
- S=20、尾部2、28→56→84→全部完整历史；不足20时保留实际条数。2月1日当前Q2预测器对应17条完整轨迹，未重复凑数。
- 只读继承第二问已完成运行 `outputs/q2/dispatch_runs/20260911_224501` 的每日负荷预测器身份及同名rolling-origin影子预测，生成48h，日内冻结；第三问不重新选负荷模型。
- 1月1—7日沿用Q2无可用负荷预测、储能停机的cold-start协议，SOC保持6000；1月8日起执行四节点优化并积累残差。初始00:00 PV观测不存在，不拿00:10未来值补齐。正式2月初SOC必须由1月真实连续回放产生。
- 用户在本次对话明确同意：终端价值用此前完整24h实际轨迹求确定性辅助继续价值，初态6000/6100 kWh，最近28个可用历史日取中位数；仅用于终端残值。单个历史日零边际价值合法，中位数若不为正则明确报错，禁止捏造正数。50/100/200 kWh扰动检查已保存。
- 原模型没有指定数值求解精度。正式默认gap=0，限时120秒未取证即保存失败并停止。15/60秒仅为本次计时预算；没有将Q2的3%授权自动扩展到Q3。
- M2每父节点至少4条才尝试二分，每子节点至少保留2条；不足则单分支，最大2×2×2。此为落实“样本不足不强分、保留最终实现不确定性”的计算规则，不根据测试期结果调参。

## 代码覆盖

| 方案内容 | 实现 | 实际验收 |
|---|---|---|
| 附件预报issue+h、10min右端点、线性/PCHIP | `data.py` | 原整点逐值一致；未来真值污染不影响当前场景 |
| Q2因果48h负荷、原点/提前量联合残差 | `data.py` | 预测器身份、冻结和轨迹完整实现边界 |
| 近期条件池、量级匹配、尾部PAM缩减 | `scenarios.py` | 窗口扩展、原始support、尾部、权重、无重复增样 |
| 共享参数精确饱和反馈MILP及有限上界 | `optimization.py`、`physics.py` | 固定策略逐场景原公式对照，覆盖SOC边界和lag模式 |
| 可行初值改进 | `incumbent.py` | 含终端残值及非对称结算的梯度有限差分核验；下界始终由LP/MILP提供 |
| 24h滚动、committed/proxy、跨日SOC | `rolling.py` | 真实1月8—9日；节点冻结、144段、费用、状态连续 |
| 18h stale/updated FIV及18点跨日分解 | `rolling.py` | 同SOC/负荷/支持集，实际反事实块费用；不修改主SOC |
| K0/K06/K0612/K061218、逐日OUV统计 | `analysis.py`、`rolling.py` | 四套开放节点均实际重求；完整年度统计入口未执行 |
| M0/M1/M2与条件树 | `multistage.py`、`analysis.py` | 小型树求得最优；真实2月1日16场景/11节点树限时求解并回放，未宣称最优 |
| 所有敏感性与sequential settlement | `analysis.py` | 9项全年单因素配置；两结算×四更新时间集实际重求及逐次费用核对 |
| 预测误差、旧新比较、相关性 | `analysis.py`、`src/plots/q3.py` | 原附件统计与方案3.24预检值吻合；热图实际加载 |
| 真实费用、题目表1—3、result3模板 | `export.py` | 四工作表回读；零紧急事件；拒绝部分日期冒充完整结果 |
| 逐日续算、冻结输入、审计摘要 | `rolling.py`、`config.py` | 9日小范围前缀续读；改动节点审计即拒绝续算 |

逐源行映射见 [source_line_acceptance.csv](source_line_acceptance.csv)，模型与源码摘要见 [traceability_audit.json](traceability_audit.json)。映射覆盖全部非空源行；它提供可定位关系，公式语义另由逐节人工审查和独立测试核对，不能用文件存在性代替数学正确性。

## 计时及限制

[可视计时报告](../../outputs/q3/raw/timing_final_20260912/report.html) · [12节点实测](../../outputs/q3/raw/timing_final_20260912/timing_samples.csv) · [60秒实例](../../outputs/q3/raw/extended_timing_20260912/timings.csv)

| 范围 | 预计节点数 | 按当前15秒短测预算的单进程计算量 |
|---|---:|---:|
| 1月预热+2—12月主模型 | 1,432 | 约6.1小时 |
| 主模型FIV两分支18h对照 | 2,004 | 约8.5小时 |
| OUV额外三套更新时间集合 | 2,148 | 约9.1小时 |
| 九项敏感性，各含FIV及全部OUV集合 | 50,256 | 约214小时 |

以上是**按已消耗预算线性外推的计算量，不是满足严格最优条件的完成时长**。18h比较与24h正式窗口也可能有不同难度。尚未含辅助标定、M2分层诊断、sequential stress和I/O；不能将“主模型约6小时”说成全套实验约6小时。完整要求即使只按15秒预算也接近10天单进程计算量。M0确定性模型明显更快：真实样本四节点各约0.0065—0.0090秒，不套用随机MILP的15秒预算。

60秒实测：6月21日12点端到端60.356秒、gap13.5297%；12月21日6点60.322秒、gap4.5799%。二者反馈映射误差均为0。严格求解耗时仍无法由这些被截断样本可靠估计，应在正式全量前明确数值精度与计算预算；本次没有放宽正式门槛。

## 挖洞审查与未完成状态

- **P0：** 本轮数学性质、因果边界与真实组件核验未发现阻断级逻辑错误。不将有限测试解释为全年所有实例已验证。
- **P1：计算可完成性尚未取证。** 主模型12/12短测和2/2延长样本未证明严格最优；真实M2的15秒诊断下界也很松，必须延长预算或另行明确允许的求解精度后才可能形成正式可靠结果。目前不能发布它们为正式最优策略。
- **P2：年末实际数据支持缺口。** 附件2截至2025年末，12月31日18点FIV共同窗口的次年部分无法回放；代码保留当日直接价值，跨日值写缺失，不填零。跨日完整配对统计为333日，全年正式主策略仍可输出334日，因为规划可使用官方已发布的次年预报和因果负荷预测。
- **P3：已修复。** 原模板购电标签整体偏移10min；原标签存审计，改为00:00—24:00的144段。扩展储能日期、保持绝对调整量口径；修复空紧急事件表数值回读。报告窄窗口表格改为独立横向滚动。

按用户要求未做：全年主模型、全年FIV/OUV、全部敏感性/正式M2诊断集及正式 `result3.xlsx`。这些均有执行入口；本次完成的是代码、受限验证和耗时诊断，不给尚未计算的全年费用、排名或策略结果。

## 运行命令

在项目根目录使用Q2同一锁定依赖环境；本机已验证，无需新装依赖。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 uv run --offline --with-requirements src/q3/requirements.txt python -m unittest src.q3.test_q3 -v
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 uv run --offline --with-requirements src/q3/requirements.txt python -m src.q3.run benchmark --seconds 15 --output outputs/q3/raw/new_timing
```

以下仅供之后明确启动全量时使用，**本次未执行**。完整实验必须带 `--experiments`；不带时仅主轨迹及其FIV、数据诊断与工作簿。默认精度0、单节点120秒，受限会显式停止并保留原因。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 uv run --offline --with-requirements src/q3/requirements.txt python -m src.q3.run full --experiments --output outputs/q3/raw/full_run
```

正式每日先保存CSV、节点策略/场景来源和FIV审计，再原子提交摘要收据。续算校验模型、代码、配置、输入、每日CSV和两份审计；不能把改变模型或精度后的后缀混入旧记录。所有文本按UTF-8保存。
