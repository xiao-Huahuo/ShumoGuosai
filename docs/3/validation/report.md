# 最终检验方案接入与当前状态

用户提供的文件已原字节归档为 [Q3_第三问_模型检验与实验方案_最终版.md](Q3_第三问_模型检验与实验方案_最终版.md)，SHA见 `source_manifest.json`。未修改原文，也未把“全部通过、M1更优”等建议性结论当成已知结果。

主计算已实际启动，目录 `outputs/q3/raw/full_priority_20260912`，使用用户确认的3%间隙与120秒切片。它从独立冻结的runtime运行，配置/源码/输入和检查点保持一致。后续检验代码修改不会阻塞主求解或使主任务恢复签名失效。主结果导出不等待任何检验。

检验以独立、低优先级进程运行于 `outputs/q3/raw/validation_20260912`。现在只做轻量预测统计和已提交主结果的只读审计；所有优化对照等待主表完成。检验不会改写主结果或反向选主参数。

## 实现对应

| 最终方案 | 实现/输出 |
|---|---|
| 正确性、动态一致性、泄漏审计 | 逐日收据/数据/节点校验；`audit_table.csv`记录真实最大残差和违反次数；正式数据未产生时标“待正式数据” |
| 预测误差 | 四发布时间×24h的MAE/RMSE/Bias、同目标旧新对比及误差相关性，科研PNG/SVG图；年末无真值目标不补零 |
| FIV/OUV | 固定18h共同支持、相同状态和参数；复用主节点初态做FIV，分别求K0/K06/K0612；表中给累计、逐日中位数、正收益比率及18点day/cont分解 |
| M0/M1 | 同设置确定性对照；费用分项、紧急电量/发生率、吞吐量、日末SOC、相对改善及逐日配对统计 |
| 核心敏感性 | 仅新增S=10/30、尾部0/4、终端0.8/1.2共6组；基准M1复用；检查费用、紧急量、首块购电、窗口末SOC、尾部保留、时间和FIV/OUV符号/排序 |
| M2 | 4—8个分层诊断日期，至多2×2×2条件树；真实成本相对差、中位数/最大绝对差，并显示最优性间隙，不预设M2更优 |
| 其余解释假设 | PCHIP、lag-one-slot、sequential均重新优化；用紧凑表比较成本/紧急量/吞吐/FIV/OUV，sequential不采用事后改账冒充重求 |

执行顺序按文档：审计 → FIV/OUV → M0 → 六组敏感性 → M2 → 三项解释假设。基础经济价值表先于后续全部敏感性产出。

## 抽样/全年口径待确认

前一条用户要求少量实验，新文档部分指标要求全年。已询问用户选择，**主计算不等待该答复**。

- `sampled`：四个题目指定日；覆盖所有模块，结果明确标注样本范围；限时解附未达标/gap，不冒充最优或全年收益。
- `full`：FIV/OUV、M0和核心/解释假设按全年回放；复用已完成M1，不重算主链。M2始终只做4—8日诊断。

待选择记录在检验目录的 `request.json`。当前为 `pending`，不会擅自启动几十条年度辅助链。无论哪种选择，重型检验都等主表完成、完整334日审计通过后才开始。

## 保存与验收

MPC/FIV按节点/分支使用120秒切片及策略/下界检查点；已完成日期/分支/任务保存收据与哈希。M2每个日期为单个有界120秒片，先保存输入、树与可行初始策略，结束后保存最佳策略/下界；若片内进程崩溃，只需重做该日期当前片，不声称保存了分支搜索树。FD逐历史日缓存。独立检验可以从相同冻结运行目录恢复。

新检验测试4项通过，覆盖6组参数与3组假设、优先级/基准复用、真实单日FIV/OUV/M0汇总、主文件未改及无正式数据时禁止“通过”。既有主计算与恢复的42项测试已通过。最终检验稿383个非空源行均有实现定位，见 `source_acceptance.csv`。

P0：有界核验未发现主数据被写入或未计算结果被填造。P1：主任务仍在运行，全年检验结果尚不存在；部分长期节点仍需多片才能降至3%。P2：抽样/全年范围待确认；样本和未达标解不能用于全年稳健性结论。原模型的年末FIV跨日缺失仍明确保留。

[主进度](../../../outputs/q3/raw/full_priority_20260912/progress.html) · [独立检验进度](../../../outputs/q3/raw/validation_20260912/report.html)
