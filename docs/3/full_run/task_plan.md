# 主计算优先、抽样实验与全量启动

## 本轮用户授权及逐项验收
1. 实验不做全年、取少数值做表 → 题目指定四日期抽样，固定场景数/终端价值几个值，明确不是全年稳健性结论。
2. 主要算力投入正式计算 → 先跑主模型365日（1月预热+334日交付），关闭全年FIV和40条全年实验队列，主结果导出后再做抽样表。
3. 所有计算要分片和中间保存 → 120秒求解切片、节点/6h执行块检查点、逐日原子提交、FD单历史日缓存、抽样任务独立收据。
4. 防止中断丢工作 → 校验输入/代码/策略与下界，异常恢复使用已保存最优可行策略；同进程切片保留搜索状态，崩溃后不声称搜索树完整恢复。
5. 准备好自动启动全量 → 验证后实际启动后台监督与主计算，查看真实进度和检查点；不再询问启动许可。

## 计划
- [x] 重读开发规范及现有实现。
- [x] 固定抽样实验方案及计算协议，补强节点/求解/FD检查点。
- [x] 故障注入、断点恢复、并发锁、结果一致性及真实UI验证。
- [x] 启动全量主计算并核验实际推进，更新文档/目录/变更记录。

## 决策与限制
- 用户只明确缩减辅助实验，主计算已获用户明确批准gap≤3%。
- 四个代表日固定为2025-03-20、06-21、09-23、12-21；主轨迹初态由1月连续回放，抽样分支从同日主轨迹初态出发。
- 不伪造实验值，不以限时可行解冒充已证明最优；所有行报告实际状态/gap和样本范围。

## 错误记录
尚无。

主任务已启动：full_priority_20260912，独立冻结runtime，监督74700/worker74704（以supervisor.json为准）。用户随后提供最终检验稿，辅助内容转docs/3/validation独立队列，不阻塞主任务。
