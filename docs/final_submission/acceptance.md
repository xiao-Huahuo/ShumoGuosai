# C题五个最终XLSX交付验收（2026-09-13）

本次只导出第一问已有正式结果并整理五个提交文件，不重新求解模型。第一问来源为当前 `outputs/q1_current` 指向的 `outputs/.q1-runs/run-ie6fkatf/processed`；第二问采用 tail_reserve_final，第三问采用 full_priority_rescue 主轨迹，第四问采用 Q42 v2 与 Q43 v3。原始模板、CSV及原四份工作簿的SHA保持不变。

| 用户要求 | 实现 | 实际验收 |
| --- | --- | --- |
| 把result1的xlsx弄出来 | `src/q1/export_submission.mjs` 导入官方模板，填入144段购电量、六段充放电量及首尾SOC；输出 `outputs/final/result1.xlsx` | 158个必填数值逐格回读；六个指定购电时段、全天电量与费用独立复算一致；两表渲染目视通过，WPS实际打开并显示正确起始时段和数值 |
| 五个xlsx放在outputs里面作为final | 新建 `outputs/final/`，另外四表按已验收正式路径逐字节复制 | 文件目录恰为五个所需XLSX；四表SHA与原件及既有验收记录一致；每份正式期334日、48096段，无缺失计划数值或Excel错误；本地打开对话框实际显示final目录全部五个文件 |
| 五个xlsx打包成zip | `outputs/final.zip`，根目录仅五个XLSX | ZIP全部成员CRC及逐成员SHA一致，共3616817字节；无临时文件、源码、图片或额外文件夹入包 |

第一问官方模板购电时段从0:10开始，至次日0:10结束，与已核验的144个日内时段错后10分钟。按照源数据右端点映射及其余四个正式导出的同一口径，将本次提交副本的标签改为00:00–00:10至23:50–24:00；原模板字节不变，原标签与新标签均记录在 `result1_export.json`。工作表名、字段、行列结构沿用模板，只为可读性扩大填值列宽并显示4位小数，不改变存储精度。

第一问复算全天购电量59482.69899835391 kWh，费用35126.948589289634元，首尾储电量均6000 kWh。模板没有全天费用汇总字段，因此不擅自加行、列或工作表；费用复算保存在 `acceptance.json`。

数值回读、源哈希、复制与ZIP验收见 `acceptance.json`。原模板及两表的成品渲染保留在本目录；WPS界面验证见 `ui_smoke.json`。WPS中只读打开，没有保存或编辑工作簿。

P0/P1：本次导出与打包无未解决问题。第三问既有489/1336个正式节点为限时可行解、未取得3%证书的限制维持原报告结论，未通过重命名或打包改变。当前三项要求全部完成，无未完成项。
