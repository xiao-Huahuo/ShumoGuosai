# 项目体积清理与验收（2026-09-13）

## 体积来源与保留原则

按本机实际目录测量，项目含隐藏目录约2.9 GiB，超过用户提到的480 MB；未提供480 MB的统计范围，不能断定其来源。主要占用来自计算结果、重复交付展开目录、历史baseline，以及两套版本不同且仍在使用的Python环境。清理前源码目录约3.7 MiB，其中约2.1 MiB是字节码缓存。

本次仅删除逐文件验证过的ZIP展开副本、可由现有Python源文件重建的字节码和macOS目录元数据；历史baseline具有追溯价值，因此采用无损压缩。正式结果、原始输入、建模文档、求解代码、原交付ZIP、当前/上一运行代和唯一历史实验均保留。

## 目录测量

单位MiB（1 MiB = 1,048,576字节），按文件 `st_blocks × 512` 统计，不跟随符号链接。`du` 还计入目录块；APFS共享块也会影响实际磁盘空闲量，因此这里报告的是目录占用。任务期间其它工作新增了第三问图包，保留该包，整体前后差额与本次操作归因节省不完全相等。

| 目录 | 清理前MiB | 清理后MiB |
|---|---:|---:|
| `outputs` | 2389.69 | 1625.23 |
| `.venv` | 267.77 | 231.39 |
| `.venv_sensitivity` | 346.40 | 307.64 |
| `src` | 3.74 | 1.61 |
| `docs` | 12.09 | 12.07 |
| `inputs` | 9.75 | 9.75 |
| `.git` | 19.54 | 19.76 |

## 实施及逐项验收

| 用户要求 | 对应操作 | 验收证据 |
|---|---|---|
| 看看现在项目体积 | 扫描所有目录，含隐藏环境、Git和outputs | 上表及同名JSON中的before/after |
| 为什么这么大 | 区分源码、依赖、正式输出、重复交付目录和历史快照 | outputs为主要来源；两环境版本不同且有进程使用 |
| 去掉完全无用内容减少体积 | 删除两份ZIP展开副本、清理缓存；无损压缩有追溯价值的旧baseline | 见下述逐文件SHA、运行环境和实际页面验证 |

- 两份重复交付目录：`数学建模_全套结果_20260913_140811/`（7297文件）与 `数学建模_问题2_问题3_问题4-2_结果包_20260913/`（32文件）。先核对ZIP成员集合、CRC及逐成员SHA-256，全部一致后删除展开目录，节省 **361.99 MiB**；原ZIP保持原路径及原字节。
- Q2旧baseline：`outputs/history/q2_baselines/pre_tail_reserve_20260912/results/` 的11977文件完整存入同目录 `results.tar.gz`，全部解压流SHA与原文件一致后移除展开目录。归档逻辑大小160.45 MiB，目录占用节省 **403.50 MiB**。原78份源码、manifest和summary保持原字节。
- 可再生缓存/系统元数据：移除3126文件，节省 **77.35 MiB**。每份Python字节码均确认相应 `.py` 存在；不删除冻结快照里的字节码。一个baseline内的 `.DS_Store` 已随归档一起移除，不重复计数。
- 共移除22432个展开/缓存文件；逐文件原路径、大小、SHA和恢复成员记录于 [压缩CSV清单](../outputs/history/cleanup_20260913.csv.gz)。扣除新归档与该清单后归因净减少 **841.77 MiB**（约840 MB，另有数KB说明文档）。
- `.gitignore` 增补 `.DS_Store` 与两套本地环境；原有Python缓存忽略项保留。本次没有提交Git或改动求解实现。

## 完整性与实际冒烟

- 清理后先核验24352个保留文件：24351个SHA不变，唯一变化是实际访问图集产生的预览服务器日志；零缺失。更新本任务文档后再次检查非日志、非本任务更新文件：**24225个SHA一致**。
- 两套环境以 `python -B` 成功导入NumPy、pandas、SciPy和HiGHS；敏感性环境另成功导入Matplotlib/PySCIPOpt。未进行不必要的全年求解或新增模型实验。
- 实际浏览器刷新问题二图集成功，6个组合图标题和链接正常，截图中的中文与图像正常显示。
- 第一问真实运行目录报告、第二问正式报告、第四问敏感性页面、baseline目录、归档和恢复说明共6项本地HTTP检查均为200。
- 旧8763预览服务响应为空，错误页又阻止了后续浏览器导航；归档目录GUI检查未通过，改用临时本地服务完成HTTP校验。问题二图集的浏览器冒烟已实际通过，未把HTTP检查冒充界面验证。

## 恢复与保留边界

旧baseline恢复命令见 [RESTORE.md](../outputs/history/q2_baselines/pre_tail_reserve_20260912/RESTORE.md)；原交付目录可解压各自原ZIP恢复。缓存由Python自动重新生成，目录体积会随后续运行增长。

两套Python环境、唯一历史实验/检查点、正式结果、Git历史和第三问新图包均保留；进一步缩小需改变保留范围，不能认定它们完全无用。清理范围内无未完成项。P0/P1新增问题为0；已知旧跨平台别名 `outputs/q1_current`、`outputs/q2_current`、`inputs/processed` 为普通文本而非符号链接/目录，本次保持其原字节，没有顺带修复。
