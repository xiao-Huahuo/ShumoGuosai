# 问题二与问题三局部敏感性实验执行协议

原文已逐字节保存为《问题2与问题3敏感性分析实验方案_时间受限版.md》，SHA256为866563d9481f002ba4feba69b1b7e113027d92f3f8d94b4df9ccac3838a3696f。原文止于第9节代码块，未擅自补写原件。

## 计算前固定的选择规则与口径

- Q2常规日：先限制紧急购电量不高于全年日中位数27.0056198kWh，再选日费用最接近全年中位数者；高误差日按正式点预测净负荷MAE最大；高紧急日按紧急量最大；高压力日按实际峰值净负荷最大。
- Q3常规日按日费用最接近全年中位数；高更新日比较正式模型所用10分钟光伏预测曲线，在相同绝对时段分别计算0:00与6:00、0:00与12:00对接下来6小时的平均绝对差；高调整日按净调整费用最大；高风险日按紧急量最大。
- 按方案表格顺序选择；重复日期顺延至该指标排名下一、未使用者；全部并列按日期升序。选择后不因实验结果改换日期。
- 每个实验只固定该代表日0:00的正式实际SOC；Q3日内后续节点SOC由该变体自己的已执行块传递。不同代表日之间不构造新的SOC链。
- 历史预测、原始误差块、原条件池、点预测与λ基准在实验前保存为独立NPZ；基准策略回放、情景目标与概率均重新核对，但不重求解基准配置。
- Q2已选K=3、S=26固定；β_R只乘最终reserve向量，不改变理论0.8分位。ρ_tail只传入原构造器的0.1/0.3，保持S26和reserve字节不变；整数尾部名额按正式构造器的既有舍入规则记录实际值。

## 用户确认的唯一方案调整

实际Q3四个代表日的16个节点，正式条件池都恰有28条历史轨迹，无法同时保持同池与构造30个真实代表。用户明确选择“保持历史池不变，场景数改为10/20/28”。因此S=30改为28；不扩大历史池、不复制轨迹。S10与S28均在固定28条池内缩减，S20基准复用原正式结果。

## 正式基准与求解精度

Q2：outputs/q2/dispatch_runs/20260912_tail_reserve_final/processed/main。读取每个代表日正式执行设置：3%间隙、单线程、120秒原预算及900秒追加预算、5秒refine；按相同模型与求解流程计算固定K/S策略。

Q3：outputs/q3/raw/full_priority_rescue_7h_20260912/main，执行配置来自该run/execution.json。使用原HiGHS1.15.1、4线程、legacy formulation、3%目标、6:00节点35秒/其他节点8秒、原限时可行接受规则。参数变化仅为S或terminal_scale；终端价值以各正式节点已缓存的λ乘0.8/1.2，不重新标定。

环境单独建立.venv_sensitivity，NumPy2.3.5、SciPy1.16.3、Pandas2.2.3、PySCIPOpt6.2.1、HiGHS1.15.1等沿用原依赖版本；不修改已完成Q4的运行环境或源码。

未达到3%的可行结果单独标记，不冒充最优证书。基准和变体的求解误差可能影响小幅成本差，报告保留这一限制。复用基准的耗时是原设备/原运行记录，不能据此作本机速度比结论。

## 范围与可选门槛

核心32项：Q2的β_R=0.8/1.2、ρ_tail=0.1/0.3，各4日；Q3的S=10/28、λ倍率0.8/1.2，各4日。基准值不重算。

Q2可选场景数实验以S*=20为前提，本次正式S26，故不适用。Q3可选尾部0/2/4仅在核心耗时不超过1小时、无失败且距18:00至少20分钟时执行，额外8项，基准tail2继续复用。这一门槛在观察敏感性结果前声明。

## 验收与输出

6项单元/冻结隔离检查通过后，以首个Q2真实变体作为核心任务内的纵向验证；其已取得3%证书，后续直接复用该结果，不重复计算。每任务保存request、输入NPZ、策略/执行CSV、节点审计和带SHA结果收据，按签名恢复。

输出完整逐日结果和聚合中位数/最小值/最大值；基准紧急量为0时仅报绝对变化。无bootstrap、无显著性检验、无全年重跑。全部对照仅解释同状态的局部参数影响，不宣称全年稳健性或理论最优参数。

右侧进度页：http://127.0.0.1:8766/，读取原子状态文件，区分已认证、限时可行与失败；不使用模拟进度。
